from setuptools import setup

setup(name='inversegraphics_generator',
      version='1.0',
      install_requires=['anytree',
                        'numpy',
                        'matplotlib',
                        'tqdm']
      )
